<p id="notice"><%= notice %></p>

<h1>User Posts</h1>

<table>
  <thead>
    <tr>
      <th>Id</th>
      <th>Name</th>
      <th>Title</th>
      <th>Body</th>
      <th>Update at</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @user_posts.each do |user_post| %>
      <tr>
        <td><%= user_post.id %></td>
        <td><%= user_post.name %></td>
        <td><%= user_post.title %></td>
        <td><%= user_post.body %></td>
        <td><%= user_post.update_at %></td>
        <td><%= link_to 'Show', user_post %></td>
        <td><%= link_to 'Edit', edit_user_post_path(user_post) %></td>
        <td><%= link_to 'Destroy', user_post, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New User Post', new_user_post_path %>
